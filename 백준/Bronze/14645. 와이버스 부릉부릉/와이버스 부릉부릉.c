#include <stdio.h>

int main(void) { 
   int a, b, c, d;
   scanf("%d %d", &a, &b);

   for(int i = 0; i < a; i++){
      scanf("%d %d", &c, &d);
   }

   printf("비와이");
   
   return 0;
}